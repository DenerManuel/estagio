import { createCardsContainer } from './CardsContainer.js'
import { createCategoriesContainer } from './CategoriesContainer.js'
import { createTooltipContainer } from './TooltipContainer.js'

export const Containers = {
  createCardsContainer: createCardsContainer,
  createCategoriesContainer: createCategoriesContainer,
  createTooltipContainer: createTooltipContainer
}